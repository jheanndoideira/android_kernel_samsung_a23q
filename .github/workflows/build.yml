name: Build A235M Final (KSU Fixed Place)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        repository: potatogeek/kernel-a235m-f
        ref: main
        path: kernel_source
        fetch-depth: 1

    - name: Instalar Dependências e Fix Python 2
      run: |
        sudo apt update -y
        sudo apt install bc cpio flex bison git gcc g++ tar perl curl libc++-dev lz4 libssl-dev python2 -y
        sudo ln -sf /usr/bin/python2 /usr/bin/python
      working-directory: .

    - name: Baixar Toolchains (Infalível)
      run: |
        mkdir toolchains && cd toolchains
        # Clang 12 (crDroid)
        echo "Baixando Clang 12..."
        git clone --depth=1 https://github.com/crdroidandroid/android_prebuilts_clang_host_linux-x86_clang-r416183b1.git clang
        # GCC 4.9 (LineageOS)
        echo "Baixando GCC..."
        git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 google
      working-directory: .

    - name: Injetar KernelSU Next (Correção de Pasta)
      run: |
        cd kernel_source
        echo "=== Baixando KernelSU Next ==="
        
        # 1. Clona em uma pasta temporária
        git clone --depth 1 https://github.com/rifsxd/KernelSU-Next temp_ksu
        
        # 2. Move APENAS o conteúdo da pasta 'kernel' para o lugar certo
        mv temp_ksu/kernel drivers/kernelsu
        
        # 3. Apaga o resto para não confundir
        rm -rf temp_ksu
        
        echo "=== Configurando KSU no Makefile ==="
        echo "obj-y += kernelsu/" >> drivers/Makefile
        sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
        
        # Verificação de segurança
        if [ -f "drivers/kernelsu/Kconfig" ]; then
            echo "SUCESSO: Arquivo Kconfig encontrado no lugar certo!"
        else
            echo "ERRO: O arquivo drivers/kernelsu/Kconfig ainda não existe."
            ls -R drivers/kernelsu
            exit 1
        fi
      working-directory: .

    - name: Injetar SUSFS 1.5.5
      run: |
        git clone https://gitlab.com/simonpunk/susfs4ksu.git
        cp susfs4ksu/kernel_patches/fs/susfs.c kernel_source/fs/
        cp susfs4ksu/kernel_patches/include/linux/susfs.h kernel_source/include/linux/
        cd kernel_source
        
        patch -p1 < ../susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.19.patch || true
        
        if ! grep -q "susfs" fs/Makefile; then 
            echo "obj-\$(CONFIG_KSU_SUSFS) += susfs.o" >> fs/Makefile
        fi
      working-directory: .

    - name: Configurar e Compilar (Target: a23_eur_open)
      run: |
        export PATH=$(pwd)/toolchains/clang/bin:$(pwd)/toolchains/google/bin:$PATH
        export ARCH=arm64
        export CROSS_COMPILE=aarch64-linux-android-
        export CLANG_TRIPLE=aarch64-linux-gnu-
        export CC=clang
        
        cd kernel_source
        
        make clean && make mrproper
        
        echo "=== CARREGANDO CONFIGURAÇÃO ==="
        # Aqui usamos o nome que descobrimos que funciona
        make vendor/a23_eur_open_defconfig
        
        echo "=== ATIVANDO PERFORMANCE ==="
        ./scripts/config --file .config \
            --enable CONFIG_KSU \
            --enable CONFIG_KSU_SUSFS \
            --enable CONFIG_KSU_SUSFS_SUS_PATH \
            --enable CONFIG_KSU_SUSFS_SUS_MOUNT \
            --enable CONFIG_JUMP_LABEL \
            --enable CONFIG_HZ_300 \
            --disable CONFIG_DEBUG_KERNEL \
            --disable CONFIG_SAMSUNG_DEBUG \
            --disable CONFIG_FTRACE \
            --disable CONFIG_PROFILING \
            --disable CONFIG_STACKPROTECTOR_STRONG
        
        make olddefconfig
        
        echo "=== COMPILANDO (Isso vai demorar 20-30 min) ==="
        export KCFLAGS="-O3 -pipe -march=armv8-a+crc+crypto"
        make -j$(nproc --all) CC=clang CROSS_COMPILE=aarch64-linux-android- CLANG_TRIPLE=aarch64-linux-gnu- LLVM=1 LLVM_IAS=1

        echo "details=A235M-PERF-$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
      working-directory: .

    - name: Criar VBMeta Disabled
      run: |
        wget https://raw.githubusercontent.com/external-avb/avb/master/avbtool.py
        python3 avbtool.py make_vbmeta_image --flags 2 --padding_size 4096 --output vbmeta_disabled.img
      working-directory: .

    - name: Enviar Tudo
      uses: actions/upload-artifact@v4
      with:
        name: ${{ steps.buildKernel.outputs.details }}
        path: |
          kernel_source/arch/arm64/boot/Image
          vbmeta_disabled.img
